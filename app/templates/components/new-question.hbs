<h2> Ask a New Question </h2>
  <div>
    <form>
      <div class="form-group">
        <label for="author">Author:</label>
        {{input class="form-control" value=author id="author" placeholder="Your name"}}
      </div>
      <div class="form-group">
        <label for="title">Title:</label>
        {{input class="form-control" value=title id="title" placeholder="Enter question"}}
      </div>
      <div class="form-group">
        <label for="body">Body:</label>
        {{textarea class="form-control" value=body id="body" placeholder="Enter additional details regarding question"}}
      </div>
      <button class="btn btn-default" {{action 'saveQuestion'}}>Ask Question</button>
    </form>
  </div>
